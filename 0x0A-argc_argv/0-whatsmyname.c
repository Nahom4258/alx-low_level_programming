#include <stdio.h>
/**
 * main - accepts argc and argv
 * @argc: no of arguments
 * @argv: the argument array
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	_putchar('\n');
	return (0);
}
